# redirect-blocker
Chrome extension that blocks redirects from specified sites, skips YouTube ads automatically, with import/export settings backup.

To use download the zip file, extract it, turn on the developer mode in chrome://extensions/ and drag the unzipped folder there.

The export button downloads a json file that contains your settings (specified sites and the option to skip youtube ads or not)
